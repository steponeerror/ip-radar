# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Node
node_modules/
dist/

# Data files (downloaded datasets)
backend/data/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Secrets
/.env.local
backend/.env
*.pem
*.key

# Git worktrees (local scratch workspaces)
.worktrees/

# Tool caches
.codegraph/
.playwright-mcp/
.pytest_cache/

# Build artifacts (regenerated by build_zip.sh)
release/app/
release/data/
release/python/
release/static/

# Windows release zip — contains live API keys (copies backend/.env).
# For LOCAL USE ONLY. Never commit/push to GitHub.
IPRadar-Windows.zip

# Debug screenshots
favicon-preview.png
page-empty.png
page-idle.png
results-final.png

# Temp files
*.tmp

# Pytest regression baseline (closed-loop campaign scratch)
.baseline-pytest.txt
baseline-pytest.txt

# Design scratch (superpowers brainstorm/plans/specs)
.superpowers/

# docs/ = nested private repository (own .git; history lives there, not here)
/docs/

# Nested separate repository (has its own .git)
misp-docker/

# 根目录 AGENTS.md 含服务器部署细节,不入库(留磁盘)
AGENTS.md
